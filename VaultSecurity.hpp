#pragma once

#include <string>
#include <exception>

/*
 * VaultSecurity Namespace
 * -----------------------
 * Define a namespace called VaultSecurity here.
 * Inside the namespace, define TWO custom exception classes:
 *
 *   1. AccessDeniedException
 *   2. VaultFullException
 *
 * Both must:
 *   - Inherit from std::exception
 *   - Have a constructor that takes a const std::string& message
 *   - Store that message in a private member variable
 *   - Override the what() function to return the message
 *
 * IMPORTANT: Do NOT change the namespace or exception class names!
 *            The auto-grader depends on these exact names.
 */

/* Define your VaultSecurity namespace and exception classes here */
